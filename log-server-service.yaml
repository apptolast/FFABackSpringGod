apiVersion: v1
kind: Service
metadata:
  name: log-server-service
spec:
  selector:
    app: log-server
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30081  # Asegúrate de que este puerto no esté en uso
